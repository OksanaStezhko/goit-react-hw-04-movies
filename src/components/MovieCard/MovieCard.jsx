import React from 'react';
import { Link } from 'react-router-dom';

const MovieCard = ({ movieInfo }) => {
  <li>{/* <Link to="/movieDetals">{movieInfo.title}</Link> */}</li>;
};

export default MovieCard;
